- 环境预检：`bash ./scripts/doctor.sh` 验证 JBR 21 及 Zig 等关键工具链状态。
- 资产初始化：支持 `bash ./scripts/init.sh`、`python3 ./scripts/init.py` 或 `node ./scripts/init.mjs` 交互式下载并部署 Wasmtime C-API 资产至 `build/platforms/`。
- 版本同步：`python3 scripts/sync_versions.py --set wasmtime_version=<ver>` 统一管理项目内分散的版本配置。