- 所有发布模块统一应用 `com.vanniktech.maven.publish` 插件并配置 Apache 2.0 许可证及 SCM 信息。
- KMP 模块普遍采用 `applyDefaultHierarchyTemplate()` 并通过创建 `hostMain`、`jniMain` 等中间源码集实现平台逻辑复用。
- 工具链模块（CLI/Gradle/Kotlin Plugin）统一使用 `buildConfig` 插件注入插件 ID、版本号等构建期常量。