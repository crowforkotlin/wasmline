---
title: 组件全览测试
description: 测试 Fumadocs 所有 UI 组件的渲染表现
---

# 文本与排版 (Typography)

这是一个普通的段落，包含 **加粗**, *斜体*, ~~删除线~~ 和 `行内代码`。

## 引用 (Blockquote)

> 这是一个引用块。
> 可以有多行。

---

# 提示框 (Callouts)

<Callout title="默认提示">
  这是一条普通的提示信息。
</Callout>

<Callout type="info" title="信息 (Info)">
  这里展示一些补充信息。
</Callout>

<Callout type="warn" title="警告 (Warning)">
  请注意，操作可能具有风险。
</Callout>

<Callout type="error" title="错误 (Error)">
  操作失败，请检查配置。
</Callout>

---

# 卡片 (Cards)

<Cards>
  <Card href="#" title="了解 Next.js" description="访问 Next.js 官方文档" />
  <Card href="#" title="了解 Fumadocs" description="查看构建文档的工具" />
</Cards>

---

# 选项卡 (Tabs)

<Tabs items={['Javascript', 'Rust', 'Go']}>
  <Tab value="Javascript">
    ```javascript
    console.log('Hello World');
    ```
  </Tab>
  <Tab value="Rust">
    ```rust
    fn main() {
        println!("Hello World");
    }
    ```
  </Tab>
  <Tab value="Go">
    ```go
    package main
    import "fmt"

    func main() {
        fmt.Println("Hello World")
    }
    ```
  </Tab>
</Tabs>

---

# 折叠面板 (Accordions)

<Accordions>
  <Accordion title="什么是 Fumadocs?">
    Fumadocs 是一个基于 Next.js 的文档构建框架。
  </Accordion>
  <Accordion title="如何安装?">
    运行 `pnpm create fumadocs-app` 即可。
  </Accordion>
</Accordions>

---

# 步骤条 (Steps)

<Steps>
  <Step>
    ### 第一步：安装依赖
    
    ```bash
    pnpm install
    ```
  </Step>
 
  <Step>
    ### 第二步：运行开发服务器
    
    ```bash
    pnpm dev
    ```
  </Step>

  <Step>
    ### 第三步：构建
    
    准备部署你的应用。
  </Step>
</Steps>

---

# 代码块 (Code Blocks)

带标题和行高亮的代码块：

```tsx title="src/app/page.tsx"
import { useMDXComponents } from '@/mdx-components'; // [!code highlight]

export default function Page() {
  return <h1>Hello World</h1>;
}
