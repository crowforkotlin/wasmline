- 使用抽象类（如 `ApplicationExtension`）作为 Gradle 扩展的载体，通过 `project.configure<T>` 方式对目标插件 DSL 进行类型安全的配置注入。
- 在 `Config` 对象中通过遍历 `Project` 父级路径动态推导模块的 `namespace` 和 `baseName`，实现基于目录结构的自动化命名规范。
- 利用 `ExperimentalKotlinGradlePluginApi` 和 `applyHierarchyTemplate` 自定义 KMP 源集分组（如 `nonWebCommon`、`native`），以优化跨平台代码共享结构。