- 公共 API 使用 sealed interface 对来源与解析结果进行代数建模，确保模式匹配完备性。
- 可扩展的解析行为通过 fun interface 定义单一方法契约，并以可选字段注入到请求中，未提供时回退默认实现。
- 数据模型统一采用 @Serializable 与 @ProtoNumber 标注，显式控制字段序号以保障二进制兼容。
- 平台差异通过 internal expect/actual 声明抽象（如签名算法、时钟、主机目标），在 commonMain 中仅保留接口与工厂逻辑。