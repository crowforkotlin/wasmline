# gradle-build.md

```shell
./gradlew :sample-plugin:wasmlineAssembleDebug
./gradlew :sample-plugin:wasmlineAssembleRelease
```

The assembled files are written to `plugins/kotlin-service/build/wasmline/output/`.
