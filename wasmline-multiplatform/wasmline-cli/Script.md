```shell
./gradlew :wasmline-cli:test
```

```shell
./gradlew :wasmline-cli:run --args="generate-key-pair"
```

```shell
./gradlew :wasmline-cli:run --args="generate-key-pair -a Ed25519"
```

```shell
./gradlew :wasmline-cli:run --args="generate-key-pair -a EcdsaP256"
```